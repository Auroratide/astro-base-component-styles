{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "publish": "rm -rf dist && npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.19",
    "gh-pages": "^3.2.3"
  }
}
